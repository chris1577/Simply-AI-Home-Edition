# Simply AI - Home Edition
# Environment Configuration Template
#
# Copy this file to .env before running the application:
#   Windows:  copy .env.example .env
#   Linux:    cp .env.example .env
#
# Then customize the values below as needed.

# =============================================================================
# FLASK CONFIGURATION
# =============================================================================

# Environment mode: development or production
# - development: Enables debug mode, verbose logging
# - production: Optimized for deployment, requires SECRET_KEY
FLASK_ENV=development

# Server port (default: 8080)
PORT=8080

# Secret key for session encryption and CSRF protection
# IMPORTANT: Generate a unique key for production!
# Generate with: python -c "import secrets; print(secrets.token_hex(64))"
SECRET_KEY=change-this-to-a-secure-random-key

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================

# Database type: sqlite (default)
# SQLite is recommended for home use - no additional setup required
DB_TYPE=sqlite

# SQLite database path (relative to instance folder)
SQLITE_URL=sqlite:///simplyai.db

# =============================================================================
# SESSION & SECURITY
# =============================================================================

# Session cookie security
# Set to True only if using HTTPS
SESSION_COOKIE_SECURE=False

# Prevent JavaScript access to session cookie (recommended: True)
SESSION_COOKIE_HTTPONLY=True

# Session lifetime in seconds (default: 86400 = 24 hours)
PERMANENT_SESSION_LIFETIME=86400

# =============================================================================
# LOGGING
# =============================================================================

# Log level: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# Log file name (stored in logs/ folder)
LOG_FILE=app.log

# =============================================================================
# NOTES
# =============================================================================
#
# API KEYS & MODEL IDs:
# API keys and AI model IDs are NOT configured here.
# They are managed through the admin Settings page and stored securely
# in the database (API keys are encrypted).
#
# To configure API keys:
# 1. Log in as a super_admin user
# 2. Navigate to Settings
# 3. Enter API keys in the "System API Keys" section
# 4. Configure model IDs in the respective sections
#
